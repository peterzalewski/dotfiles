---
- defaults:
    link:
      create: true
      force: true

- clean:
    '~': {}
    ~/.config:
      recursive: true
    ~/.zsh.d:
      recursive: true
    ~/.local/share/nvim/site:
      recursive: true

- create:
    ~/bin: {}
